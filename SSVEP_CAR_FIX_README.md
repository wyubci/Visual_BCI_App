# SSVEP 脑控小车系统修正版说明

## 1. 这次主要修正了什么

本次修正重点解决 **165 Hz 屏幕下刺激频率与识别模型参考频率不一致** 的问题。原系统使用 6.67、7.50、8.57、12.00、15.00 Hz 等名义频率，并通过正弦函数按时间采样切换黑白块。但在 165 Hz 显示器上，很多频率无法按整帧周期稳定显示，例如 6.67 Hz 对应约 24.74 帧/周期，不是整数帧，实际刺激会抖动，CCA、FBCCA、TDCA 构造的参考信号也会与真实视觉刺激不一致，容易导致在线准确率很差。

修正版采用 **屏幕刷新率自适应 + 整数帧周期刺激**：

- 自动读取当前屏幕刷新率。
- 根据刷新率选择整数帧周期。
- 用 `实际频率 = 屏幕刷新率 / 帧周期` 作为刺激频率。
- CCA、FBCCA、TDCA 全部使用同一组实际频率构造参考信号。
- 训练数据、在线测试数据、权重文件都会记录实际刺激频率和屏幕刷新率，防止误用旧数据或旧权重。

如果屏幕是 165 Hz，当前 5 个命令的实际频率为：

| 命令 | 帧周期 | 实际频率 |
|---|---:|---:|
| 前进 | 24 帧 | 6.875 Hz |
| 后退 | 22 帧 | 7.500 Hz |
| 左转 | 20 帧 | 8.250 Hz |
| 停止 | 16 帧 | 10.3125 Hz |
| 右转 | 12 帧 | 13.750 Hz |

## 2. 为什么旧系统可能效果很差

我检查后认为主要风险有这些：

1. **刺激频率不是屏幕整帧频率**  
   这是最大问题。SSVEP 是视觉刺激驱动的，如果真实闪烁频率不稳定，后面的 CCA、FBCCA、TDCA 再怎么调权重都很难稳定。

2. **旧训练数据和当前屏幕频率可能不一致**  
   原来采集的数据可能是旧频率、旧屏幕或旧流程下的样本。如果换到 165 Hz 后继续加载旧权重，模型相当于用旧频率去识别新频率，会显著降低准确率。

3. **TDCA 依赖被试训练数据**  
   TDCA 不是完全免训练算法。每个命令至少要有足够稳定的训练样本，建议每类不少于 10 条，正式使用建议每类 15 到 20 条。

4. **在线控制不应直接用未验证模型驱动车**  
   应先做离线测评和屏幕内在线测试，确认准确率、混淆矩阵和每类识别都正常，再打开小车控制。

## 3. 推荐使用流程

### 第一步：固定显示设置

- Windows 设置中确认显示器刷新率为 165 Hz。
- 关闭动态刷新率、可变刷新率、游戏模式中的变帧率相关设置。
- 程序运行时尽量全屏，避免拖动窗口和遮挡闪烁块。
- 屏幕亮度保持较高且稳定。

### 第二步：重新采集训练数据

不要直接用旧数据训练。进入小车界面后，按当前 165 Hz 频率重新采集。

建议：

- 刺激时间：先用 4.0 s。
- 每类样本：至少 10 条，建议 15 到 20 条。
- 采集顺序：不要一直采同一类，建议前进、后退、左转、停止、右转循环采。
- 采集时眼睛只看目标方块，尽量减少眨眼、转头和说话。

### 第三步：评分与筛选

在“训练数据与权重管理”中：

1. 刷新数据。
2. 评分并勾选。
3. 先看 FBCCA、CCA、TDCA 对训练数据的命中情况。
4. 如果某一类总是被识别成另一类，优先重采这两类数据。

### 第四步：训练权重并加载

- 勾选高质量样本后点击训练权重。
- 新权重文件会写入当前实际刺激频率。
- 如果屏幕刷新率或刺激频率不一致，程序会拒绝加载不兼容权重。

### 第五步：先屏幕内测试，再接小车

正式控制小车前，先做在线测试：

- 只看目标，不连接或不执行小车动作。
- 看在线准确率、最近投票、每类误识别情况。
- 准确率稳定后再连接小车。

## 4. 文件级修改摘要

- `interface/car_interface/car_window.py`  
  增加屏幕刷新率检测、整数帧刺激频率生成、刺激元数据保存、旧数据/旧权重频率兼容检查、界面状态栏显示真实刺激频率。

- `interface/car_interface/training_framework.py`  
  训练时检查样本的实际刺激频率是否与当前屏幕一致，权重文件写入 `stim_freqs_hz`。

- `models/FBCCA.py`  
  修正滤波器组对通道数的硬编码问题，增强不同通道数输入的兼容性，并处理 CCA 相关系数中的 NaN。

- `models/CCA.py`  
  使用零相位滤波，降低滤波相位偏移对相关性计算的影响。

- `main.py`  
  不再每次启动强制把被试名改为 `hc33`，避免数据混到同一个被试目录。

## 5. 仍然需要你现场确认的内容

代码已经按流程修正，但硬件现场还要确认：

- EEG 采集设备实际采样率是不是 250 Hz。
- 接收数据的通道顺序是否稳定。
- 采集缓存中是否有大量丢包或时间戳不连续。
- 每个 trial 的刺激开始时间和数据切片是否能覆盖完整 4 s。
- 小车控制网络延迟是否会造成动作滞后。

如果修正后准确率仍低，优先排查顺序是：屏幕实际刷新率 → 重新采集 4 s 数据 → 单类频谱是否在目标频率及谐波有峰 → FBCCA 离线准确率 → TDCA 留一法准确率 → 在线投票阈值。

## 6. 数据目录说明

修正版统一使用项目根目录下的：

```text
saveCarData/<被试名>/train/
saveCarData/<被试名>/weights/
```

这样无论从 PyCharm、VS Code 还是命令行启动，只要代码文件位置不变，训练数据和权重目录都一致。原压缩包中的 `runtime_data/saveCarData` 已复制到项目根目录 `saveCarData`，方便继续查看旧文件。但如果你的屏幕已经改为 165 Hz，旧频率数据和旧权重不建议继续用于正式控制。
