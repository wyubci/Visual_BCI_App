# 脑控无人机修改总结

## 修改需求
1. 按下空格键后开始一次4秒的刺激，刺激完后不再继续执行刺激
2. 按一下M键，执行一次刺激界面，算法只识别4分类：前进、后退、左移、右移
3. M键模式的移动距离可以单独设置，不与空格键模式混合
4. **新增**：将刺激界面从12个刺激改为8个刺激（删除最后一行的4个刺激）
5. **新增**：去除2秒等待时间，按键后立即开始刺激

## 主要修改文件

### 1. config/configer.py
- **添加内容**: M键模式距离配置
```python
# M键模式下的移动距离设置
self.m_mode_distances = {
    'forward': 100,   # 前进距离(厘米)
    'backward': 100,  # 后退距离(厘米)
    'left': 100,      # 左移距离(厘米)
    'right': 100      # 右移距离(厘米)
}
```

### 2. models/FBCCA.py
- **添加内容**: 4分类识别方法
```python
def classify_4_class(self, test_data):
    """对测试数据进行4分类SSVEP频率识别（仅识别前4个频率）"""
```

### 3. interface/drone_interface/drone_window.py
#### 主要修改:
1. **添加模式标志**: `self.is_m_mode = False`
2. **修改键盘事件处理**:
   - 空格键: 8分类模式
   - M键: 4分类模式
   - 回车键: 兼容旧版本，8分类模式
3. **修改刺激逻辑**: 单次执行，去除循环
4. **修改识别逻辑**: 根据模式调用不同算法
5. **添加距离配置方法**: `set_m_mode_distances()` 和 `get_m_mode_distances()`
6. **界面布局修改**: 删除第三行的4个刺激块，只保留前两行的8个刺激
7. **频率列表调整**: 从12个频率减少为8个频率：`[8, 8.5, 9, 9.5, 10.2, 10.8, 11.2, 11.5]`
8. **命令列表调整**: 从12个命令减少为8个命令：`["起飞", "降落", "上升", "下降", "前进", "后退", "左移", "右移"]`

### 4. interface/drone_interface/drone_control.py
#### 主要修改:
1. **动作命令映射**: 删除索引8-12的动作，只保留0-7的8个动作

## 功能说明

### 空格键模式 (8分类)
- 按空格键开始刺激
- 执行完整的8分类识别
- 可以执行8种动作：起飞、降落、上升、下降、前进、后退、左移、右移
- 使用原有的距离设置

### M键模式 (4分类)
- 按M键开始刺激
- 只识别前4个频率对应的动作：
  - 频率1 → 前进 (动作索引4)
  - 频率2 → 后退 (动作索引5)
  - 频率3 → 左移 (动作索引6)
  - 频率4 → 右移 (动作索引7)
- 使用独立的距离设置
- 其他4个刺激区域仍会闪烁，但只有前4个参与识别

### 界面布局变化
- **之前**: 3行4列，共12个刺激块
- **现在**: 2行4列，共8个刺激块
- **删除的刺激**: 翻转、后二、左45、右90（原来的第三行）

### 刺激流程
1. 按键触发 → 立即开始4秒刺激 → 算法识别 → 执行动作
2. 每次按键只执行一次完整流程，无等待时间
3. 需要再次按键才能进行下一次刺激

## 使用示例

### 设置M键模式距离
```python
# 在无人机窗口中
drone_window.set_m_mode_distances(
    forward=120,    # 前进120厘米
    backward=80,    # 后退80厘米
    left=100,       # 左移100厘米
    right=100       # 右移100厘米
)

# 获取当前设置
distances = drone_window.get_m_mode_distances()
print(distances)
```

### 测试方法
运行测试脚本：
```bash
python test_drone_modifications.py
```

## 技术细节

### 频率映射 (M键模式)
- 使用前4个刺激频率：[8, 8.5, 9, 9.5]
- 映射关系：
  ```
  识别结果0 → 动作4 (前进)
  识别结果1 → 动作5 (后退)
  识别结果2 → 动作6 (左移)
  识别结果3 → 动作7 (右移)
  ```

### 算法优化
- M键模式只计算前4个频率的相关性
- 减少计算量，提高识别速度
- 空格键模式现在计算8个频率，比之前的12个频率更高效

### 界面简化
- 删除了复杂的翻转、旋转等高级动作
- 专注于基本的飞行控制：起飞、降落、移动
- 界面更加简洁，减少用户认知负担

## 注意事项
1. 确保在刺激期间不要重复按键
2. M键模式的距离设置会保存到配置文件
3. 两种模式完全独立，不会相互影响
4. 保持了向后兼容性，原有的基本功能不受影响
5. 界面现在只有8个刺激块，布局更加紧凑
6. 🆕 按键后立即开始刺激，无需等待，响应更加迅速 